# VITYARTHI_Programming_in_Java
This report describes the current version of the Book Inventory System, which uses an in-memory Array-List rather than a database. Therefore, an ER diagram is not applicable to the current implementation. The project can later be extended with permanent database storage, authentication, and additional inventory features.
